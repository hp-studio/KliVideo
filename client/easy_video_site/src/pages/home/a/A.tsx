import React from 'react';
import {VideoPlayer} from '../../../components';

export const A = () => {
    return (
        <div className='content' style={{padding: '1rem'}}>
            <VideoPlayer />
        </div>
    );
};
