import React from 'react';
import {VideoPlayer} from '../../../components';

export const A = () => {
    return (
        <div className='content'>
            <VideoPlayer />
        </div>
    );
};
